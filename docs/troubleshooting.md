# Troubleshooting

Use `scripts/check_system.sh`, then Diagnostics. CUDA out-of-memory causes the configured CPU fallback load. An unavailable Codex CLI only fails its own run; transcription remains active. For migration checks run `make migrate` and use Diagnostics > Validate migrations.
