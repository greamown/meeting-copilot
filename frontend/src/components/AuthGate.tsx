import { useQuery, useQueryClient } from "@tanstack/react-query";
import { LockKeyhole } from "lucide-react";
import { FormEvent, ReactNode, useState } from "react";
import { getAuthStatus, post } from "../lib/api";

function LoginForm(){const client=useQueryClient();const [username,setUsername]=useState("admin");const [password,setPassword]=useState("");const [error,setError]=useState("");const login=async(event:FormEvent)=>{event.preventDefault();setError("");try{await post("/auth/login",{username,password});await client.invalidateQueries({queryKey:["auth-status"]})}catch(reason){setError(reason instanceof Error?reason.message:"Login failed")}};return <form className="auth-panel" onSubmit={event=>void login(event)}><LockKeyhole/><h1>Meeting Copilot</h1><p>此網路位址需要管理員登入。</p><label>使用者名稱<input autoComplete="username" required value={username} onChange={e=>setUsername(e.target.value)}/></label><label>密碼<input autoComplete="current-password" type="password" required minLength={12} value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="alert error">{error}</div>}<button className="button primary">登入</button></form>}

export function AuthGate({children}:{children:ReactNode}){const status=useQuery({queryKey:["auth-status"],queryFn:getAuthStatus,retry:false});if(status.isPending)return <main className="auth-screen"><div className="auth-panel">檢查存取權限</div></main>;if(status.isError)return <main className="auth-screen"><div className="auth-panel"><LockKeyhole/><h1>無法驗證存取權限</h1><p>{status.error.message}</p></div></main>;if(status.data.authentication_required&&!status.data.configured)return <main className="auth-screen"><div className="auth-panel"><LockKeyhole/><h1>尚未設定管理員</h1><p>請先從主機開啟 https://localhost/access 建立管理員密碼，再回到此位址登入。</p></div></main>;if(status.data.authentication_required&&!status.data.authenticated)return <main className="auth-screen"><LoginForm/></main>;return children;}
