import { useQuery } from "@tanstack/react-query";
import { Activity, Download, FlaskConical, RefreshCw, Terminal } from "lucide-react";
import { post, request } from "../lib/api";

interface DiagnosticsData { os:string; python_version:string; docker_available:boolean; ffmpeg_available:boolean; codex:{installed:boolean;authenticated:boolean;version:string|null};gpu:{available:boolean;gpus:Array<{name:string;memory_total_mb:number;memory_used_mb:number;utilization_percent:number}>};database:{healthy:boolean;latency_ms:number};metrics:{active_meetings:number;codex_queue_depth:number;audio_chunks:number};providers:Array<{id:string;name:string;health:string;latency_ms:number|null}>;events:Array<{id:string;sequence:number;type:string;source:string;created_at:string;payload:unknown}>; }
export function Diagnostics(){const diagnostics=useQuery({queryKey:["diagnostics"],queryFn:()=>request<DiagnosticsData>("/diagnostics"),refetchInterval:10000});const data=diagnostics.data;const bundle=()=>{const anchor=document.createElement("a");anchor.href="/api/diagnostics/bundle";anchor.click()};return <div className="page"><header className="page-head"><div><p className="eyebrow">OBSERVABILITY</p><h1>系統診斷</h1><p>所有事件與錯誤在輸出前皆經過秘密遮罩。</p></div><div className="head-actions"><button className="icon-button" title="重新整理" onClick={()=>void diagnostics.refetch()}><RefreshCw/></button><button className="button" onClick={bundle}><Download/>診斷包</button></div></header><div className="metric-grid compact"><article className="metric"><Activity/><span>Active meetings</span><strong>{data?.metrics.active_meetings??"—"}</strong></article><article className="metric"><Terminal/><span>Codex queue</span><strong>{data?.metrics.codex_queue_depth??"—"}</strong></article><article className="metric"><FlaskConical/><span>DB latency</span><strong>{data?`${data.database.latency_ms} ms`:"—"}</strong></article><article className="metric"><Activity/><span>Audio chunks</span><strong>{data?.metrics.audio_chunks??"—"}</strong></article></div><div className="diagnostic-grid"><section className="section-card"><h2>Providers</h2>{data?.providers.map(item=><div className="system-row" key={item.id}><span>{item.name}</span><strong>{item.health} {item.latency_ms&&`· ${item.latency_ms} ms`}</strong><button className="button" onClick={()=>void post(`/providers/${item.id}/test`).then(()=>diagnostics.refetch())}>測試</button></div>)}</section><section className="section-card"><h2>測試動作</h2><div className="button-stack"><button className="button" onClick={()=>void post("/codex/test").then(value=>alert(JSON.stringify(value,null,2)))}>固定 Codex JSON task</button><button className="button" onClick={()=>{speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance("Diagnostic speech test"))}}>播放測試語音</button><button className="button" onClick={()=>void post("/diagnostics/migrations").then(value=>alert(JSON.stringify(value,null,2)))}>驗證 migrations</button></div></section></div><section className="event-log"><header><h2>最近 100 筆事件</h2><code>sanitized</code></header>{data?.events.map(item=><div key={item.id}><time>{new Date(item.created_at).toLocaleTimeString()}</time><span>#{item.sequence}</span><strong>{item.type}</strong><code>{item.source}</code><pre>{JSON.stringify(item.payload)}</pre></div>)}</section></div>}
