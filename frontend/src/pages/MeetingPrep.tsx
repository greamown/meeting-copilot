import { useQuery } from "@tanstack/react-query";
import { Mic, Play, ShieldCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { useNavigate } from "react-router-dom";
import { getProviders, Meeting, post } from "../lib/api";
import { useMicrophone } from "../hooks/useMicrophone";

export function MeetingPrep(){const navigate=useNavigate();const providers=useQuery({queryKey:["providers"],queryFn:getProviders});const mic=useMicrophone();const [form,setForm]=useState({title:"",goal:"",language:"zh",stt_provider_id:"local-stt-primary",tts_provider_id:"browser-tts",codex_profile:"",automatic_analysis_enabled:true,analysis_interval_seconds:120,suggestion_cooldown_seconds:180,human_approval_before_speech:true,save_audio:false,repository_context_enabled:false,repository_path:"",repository_read_only:true,reference_notes:"",participants:"",privacy_acknowledged:false});const [error,setError]=useState("");
  const submit=async(event:FormEvent)=>{event.preventDefault();setError("");try{const meeting=await post<Meeting>("/meetings",{...form,participants:form.participants.split(",").map(v=>v.trim()).filter(Boolean),repository_path:form.repository_path||null});await post(`/meetings/${meeting.id}/start`);navigate(`/meetings/${meeting.id}`)}catch(reason){setError(reason instanceof Error?reason.message:"建立失敗")}};
  const field=<K extends keyof typeof form>(key:K,value:(typeof form)[K])=>setForm(current=>({...current,[key]:value}));
  return <div className="page"><header className="page-head"><div><p className="eyebrow">MEETING PREPARATION</p><h1>準備新會議</h1><p>確認隱私、音訊與分析政策後才開始串流。</p></div></header><form className="prep" onSubmit={event=>void submit(event)}><section><h2>會議內容</h2><div className="form-grid"><label className="wide">標題<input required maxLength={200} value={form.title} onChange={e=>field("title",e.target.value)} placeholder="例如：Q3 系統架構決策"/></label><label className="wide">目標<textarea required rows={3} value={form.goal} onChange={e=>field("goal",e.target.value)} placeholder="這場會議需要完成什麼決定？"/></label><label>語言<select value={form.language} onChange={e=>field("language",e.target.value)}><option value="zh">中文（含英文術語）</option><option value="en">English</option><option value="ja">日本語</option></select></label><label>參與者（逗號分隔）<input value={form.participants} onChange={e=>field("participants",e.target.value)}/></label><label className="wide">參考筆記<textarea rows={4} value={form.reference_notes} onChange={e=>field("reference_notes",e.target.value)}/></label></div></section><section><h2>音訊與模型</h2><div className="form-grid"><label>STT<select value={form.stt_provider_id} onChange={e=>field("stt_provider_id",e.target.value)}>{providers.data?.filter(p=>p.role==="stt").map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label>TTS<select value={form.tts_provider_id} onChange={e=>field("tts_provider_id",e.target.value)}>{providers.data?.filter(p=>p.role==="tts").map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label>Codex profile<input value={form.codex_profile} onChange={e=>field("codex_profile",e.target.value)}/></label><label>分析間隔（秒）<input type="number" min="30" value={form.analysis_interval_seconds} onChange={e=>field("analysis_interval_seconds",Number(e.target.value))}/></label><label>建議冷卻（秒）<input type="number" min="0" value={form.suggestion_cooldown_seconds} onChange={e=>field("suggestion_cooldown_seconds",Number(e.target.value))}/></label></div><div className="meter"><i style={{width:`${mic.level*100}%`}}/></div><button type="button" className="button" onClick={()=>mic.active?mic.stop():void mic.start()}><Mic size={16}/>{mic.active?"停止麥克風測試":"麥克風測試"}</button>{mic.error&&<span className="field-error">{mic.error}</span>}</section><section><h2>政策與隱私</h2><div className="toggle-list"><label><input type="checkbox" checked={form.automatic_analysis_enabled} onChange={e=>field("automatic_analysis_enabled",e.target.checked)}/><span>自動分析</span><small>規則觸發且通過 cooldown 才執行 Codex</small></label><label><input type="checkbox" checked={form.human_approval_before_speech} onChange={e=>field("human_approval_before_speech",e.target.checked)}/><span>語音播放前需要人員點擊</span><small>建議保留啟用</small></label><label><input type="checkbox" checked={form.save_audio} onChange={e=>field("save_audio",e.target.checked)}/><span>保存原始音訊</span><small>預設關閉；逐字稿仍會保存</small></label><label><input type="checkbox" checked={form.repository_context_enabled} onChange={e=>field("repository_context_enabled",e.target.checked)}/><span>啟用 repository context</span><small>必須位於後端 allowlist 且唯讀</small></label>{form.repository_context_enabled&&<label className="path-field">Repository path<input required value={form.repository_path} onChange={e=>field("repository_path",e.target.value)}/></label>}<label className="consent"><input required type="checkbox" checked={form.privacy_acknowledged} onChange={e=>field("privacy_acknowledged",e.target.checked)}/><ShieldCheck/><span>我已告知參與者麥克風音訊將在本機轉錄，並同意上述保存設定。</span></label></div></section>{error&&<div className="alert error">{error}</div>}<footer className="form-footer"><button type="button" className="button" onClick={()=>navigate("/")}>取消</button><button className="button primary"><Play size={16}/>開始會議</button></footer></form></div>}
