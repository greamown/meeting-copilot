import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Bot, Check, Clipboard, Edit3, Mic, Pause, Pin, Play, Search, Send, Square, Volume2, X } from "lucide-react";
import { FormEvent, useCallback, useEffect, useMemo, useRef, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import { csrfHeaders, getMeeting, patch, post, Suggestion } from "../lib/api";
import { useMicrophone } from "../hooks/useMicrophone";
import { useDialogs } from "../components/DialogProvider";

export function LiveMeeting(){const dialogs=useDialogs();const {id=""}=useParams();const navigate=useNavigate();const queryClient=useQueryClient();const detail=useQuery({queryKey:["meeting",id],queryFn:()=>getMeeting(id),refetchInterval:5000});const mic=useMicrophone();const audioSocket=useRef<WebSocket|null>(null);const audioReconnect=useRef<number|null>(null);const recording=useRef(false);const playback=useRef<HTMLAudioElement|null>(null);const sequence=useRef(0);const [events,setEvents]=useState<Array<Record<string,unknown>>>([]);const [query,setQuery]=useState("");const [question,setQuestion]=useState("");const [autoScroll,setAutoScroll]=useState(true);const [elapsed,setElapsed]=useState(0);const [editing,setEditing]=useState<{id:string;text:string}|null>(null);const [job,setJob]=useState<{run_id:string;status:string}|null>(null);const [speakingId,setSpeakingId]=useState<string|null>(null);const [audioError,setAudioError]=useState<string|null>(null);
  const refresh=useCallback(()=>void queryClient.invalidateQueries({queryKey:["meeting",id]}),[id,queryClient]);useEffect(()=>{let closed=false;let ws:WebSocket|null=null;let timer:number|null=null;let attempt=0;const connect=()=>{const protocol=location.protocol==="https:"?"wss":"ws";ws=new WebSocket(`${protocol}://${location.host}/api/meetings/${id}/events`);ws.onopen=()=>{attempt=0;ws?.send("subscribe");refresh()};ws.onmessage=event=>{const item=JSON.parse(event.data) as Record<string,unknown>;setEvents(old=>[...old.slice(-99),item]);if(String(item.type).startsWith("transcript.")||String(item.type).startsWith("suggestion.")||String(item.type).startsWith("codex."))refresh()};ws.onclose=()=>{if(!closed){timer=window.setTimeout(connect,Math.min(1000*2**attempt++,15000))}}};connect();return()=>{closed=true;if(timer!==null)clearTimeout(timer);ws?.close()}},[id,refresh]);useEffect(()=>{const started=detail.data?.meeting.started_at;if(!started)return;const tick=()=>setElapsed(Math.max(0,Math.floor((Date.now()-new Date(started).getTime())/1000)));tick();const timer=setInterval(tick,1000);return()=>clearInterval(timer)},[detail.data?.meeting.started_at]);
  const connectAudio=useCallback(async()=>{const protocol=location.protocol==="https:"?"wss":"ws";const ws=new WebSocket(`${protocol}://${location.host}/api/meetings/${id}/audio`);ws.binaryType="arraybuffer";audioSocket.current=ws;await new Promise<void>((resolve,reject)=>{ws.onopen=()=>resolve();ws.onerror=()=>reject(new Error("無法連線到音訊服務"))});ws.onclose=()=>{if(recording.current){audioReconnect.current=window.setTimeout(()=>void connectAudio(),1000)}}},[id]);const startAudio=async()=>{recording.current=true;setAudioError(null);try{await connectAudio();await mic.start(undefined,chunk=>{const ws=audioSocket.current;if(!ws||ws.readyState!==WebSocket.OPEN)return;const framed=new Uint8Array(chunk.byteLength+8);new DataView(framed.buffer).setBigUint64(0,BigInt(sequence.current++),true);framed.set(new Uint8Array(chunk),8);ws.send(framed)})}catch(reason){recording.current=false;audioSocket.current?.close();audioSocket.current=null;setAudioError(reason instanceof Error?reason.message:"無法開始錄音")}};const stopAudio=()=>{recording.current=false;if(audioReconnect.current!==null)clearTimeout(audioReconnect.current);audioReconnect.current=null;mic.stop();audioSocket.current?.close();audioSocket.current=null};useEffect(()=>stopAudio,[]);
  const command=async(action:"pause"|"resume"|"end")=>{if(action!=="resume")stopAudio();await post(`/meetings/${id}/${action}`);refresh();if(action==="end")navigate(`/history/${id}`)};const ask=async(event:FormEvent)=>{event.preventDefault();const result=await post<{run_id:string;status:string}>(`/meetings/${id}/ask`,{question,repository_context:false});setJob(result);setQuestion("")};const addManualSuggestion=async()=>{const content=await dialogs.prompt({title:"新增人工建議",message:"加入一則由與會者提出的建議。",label:"建議內容",confirmLabel:"新增"});if(content)await post(`/meetings/${id}/suggestions`,{content,category:"other",reason:"Added by a meeting participant"}).then(refresh)};const suggestions=detail.data?.suggestions??[];const displayLanguage=String(detail.data?.meeting.configuration_json.transcript_display_language??"original");const transcripts=(detail.data?.transcripts??[]).filter(item=>`${item.text} ${item.translated_text??""}`.toLowerCase().includes(query.toLowerCase()));const status=detail.data?.meeting.status;
  useEffect(()=>{const list=document.querySelector<HTMLElement>(".transcript-list");if(autoScroll&&list)list.scrollTop=list.scrollHeight},[autoScroll,transcripts.length]);
  const stopSpeech=()=>{speechSynthesis.cancel();playback.current?.pause();playback.current=null;setSpeakingId(null)};const speak=async(item:Suggestion)=>{if(item.status==="ignored")return;if(speakingId===item.id){stopSpeech();return}stopSpeech();const response=await fetch(`/api/suggestions/${item.id}/speak`,{method:"POST",credentials:"same-origin",headers:csrfHeaders()});if(!response.ok)throw new Error("TTS failed");setSpeakingId(item.id);if(response.headers.get("content-type")?.includes("application/json")){const payload=await response.json() as {text:string;language:string;voice:string;rate:number;volume:number};const utterance=new SpeechSynthesisUtterance(payload.text);utterance.lang=payload.language;utterance.rate=payload.rate;utterance.volume=payload.volume;const voice=speechSynthesis.getVoices().find(value=>value.name===payload.voice||value.lang.startsWith(payload.language));if(voice)utterance.voice=voice;utterance.onend=()=>setSpeakingId(null);utterance.onerror=()=>setSpeakingId(null);speechSynthesis.speak(utterance)}else{const url=URL.createObjectURL(await response.blob());const audio=new Audio(url);playback.current=audio;audio.onended=()=>{URL.revokeObjectURL(url);playback.current=null;setSpeakingId(null)};audio.onerror=()=>{URL.revokeObjectURL(url);playback.current=null;setSpeakingId(null)};await audio.play()}};useEffect(()=>stopSpeech,[]);const fmt=(seconds:number)=>`${String(Math.floor(seconds/3600)).padStart(2,"0")}:${String(Math.floor(seconds%3600/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;
  return <div className="live"><header className="live-head"><div><span className={`record-dot ${mic.active?"active":""}`}/><div><h1>{detail.data?.meeting.title??"載入中"}</h1><span>{fmt(elapsed)} · {status}</span></div></div><div className="live-status"><span>STT {audioError?"ERROR":mic.active?"STREAMING":"READY"}</span><span>CODEX {job?.status??"IDLE"}</span><span>QUEUE 0</span></div><div className="head-actions">{!mic.active&&status==="active"&&<button className="button" title="開始錄音" onClick={()=>void startAudio()}><Mic/>開始錄音</button>}{status==="active"?<button className="icon-button" title="暫停" onClick={()=>void command("pause")}><Pause/></button>:status==="paused"&&<button className="icon-button" title="繼續" onClick={()=>void command("resume")}><Play/></button>}<button className="button danger" onClick={()=>void command("end")}><Square size={14}/>結束</button></div></header><div className="live-grid"><section className="transcript-panel"><div className="panel-head"><h2>即時逐字稿</h2><div className="search"><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜尋"/></div><label className="compact-toggle"><input type="checkbox" checked={autoScroll} onChange={e=>setAutoScroll(e.target.checked)}/>自動捲動</label></div>{(audioError||mic.error)&&<div className="alert error">{audioError||mic.error}</div>}<div className="transcript-list">{transcripts.length===0&&<div className="empty"><Mic/><strong>{mic.active?"正在聆聽，約 4 秒後顯示字幕":"按下開始錄音以接收逐字稿"}</strong></div>}{transcripts.map(item=><article key={item.id} className={item.is_pinned?"pinned":""}><time>{fmt(Math.floor(item.start_ms/1000))}</time><div><span>{item.speaker_id??"Speaker"} · {item.language}</span>{editing?.id===item.id?<form onSubmit={e=>{e.preventDefault();void patch(`/transcripts/${item.id}`,{text:editing.text,speaker_id:item.speaker_id}).then(()=>{setEditing(null);refresh()})}}><textarea value={editing.text} onChange={e=>setEditing({...editing,text:e.target.value})}/><button className="button">儲存</button></form>:<><p>{displayLanguage!=="original"&&item.translated_language===displayLanguage&&item.translated_text?item.translated_text:item.text}</p>{item.translated_text&&<p className="translation">{displayLanguage==="original"?item.translated_text:item.text}<small>{item.translated_language}</small></p>}</>}</div><div className="row-tools"><button title="編輯原文" onClick={()=>setEditing({id:item.id,text:item.text})}><Edit3/></button><button title="釘選" onClick={()=>void patch(`/transcripts/${item.id}`,{text:item.text,speaker_id:item.speaker_id,is_pinned:!item.is_pinned}).then(refresh)}><Pin/></button></div></article>)}</div></section><aside className="suggestion-panel"><div className="panel-head"><h2>AI 建議</h2><div className="button-row"><button className="button" onClick={()=>void addManualSuggestion()}>人工建議</button><button className="button" onClick={()=>void post(`/meetings/${id}/analyze`).then(value=>setJob(value as {run_id:string;status:string}))}><Bot size={15}/>立即分析</button></div></div><div className="suggestions">{suggestions.length===0&&<div className="empty small"><Bot/><strong>尚無建議</strong><span>Codex 只在有實質新價值時顯示內容。</span></div>}{suggestions.map(item=><article key={item.id}><header><span>{item.category.replaceAll("_"," ")}</span><strong>{Math.round(item.confidence*100)}%</strong></header><p>{item.content}</p><small>{item.reason}</small><div className="suggestion-actions"><button title="接受" onClick={()=>void post(`/suggestions/${item.id}/accept`).then(refresh)}><Check/></button><button title="忽略" onClick={()=>void post(`/suggestions/${item.id}/ignore`).then(refresh)}><X/></button><button title="複製" onClick={()=>void navigator.clipboard.writeText(item.content)}><Clipboard/></button><button title="編輯建議" onClick={()=>void dialogs.prompt({title:"編輯 AI 建議",message:"更新建議內容後儲存。",label:"建議內容",initialValue:item.content,confirmLabel:"儲存"}).then(content=>content&&post(`/suggestions/${item.id}/edit`,{content}).then(refresh))}><Edit3/></button><button title={speakingId===item.id?"停止播放":"播放"} onClick={()=>void speak(item)} disabled={item.status==="ignored"}><Volume2/></button>{([ ["decision","to-decision"], ["open question","to-question"], ["risk","to-risk"], ["action item","to-action"] ] as const).map(([label,route])=><button className="text-action" key={route} onClick={()=>void post(`/suggestions/${item.id}/${route}`).then(refresh)}>+ {label}</button>)}</div></article>)}</div><form className="ask" onSubmit={event=>void ask(event)}><label>ASK CODEX</label><textarea required value={question} onChange={e=>setQuestion(e.target.value)} placeholder="針對目前討論提出問題…"/><div><span>{job&&`${job.status} · ${job.run_id.slice(0,8)}`}</span>{job&&<button type="button" className="button" onClick={()=>void post(`/codex-runs/${job.run_id}/cancel`).then(()=>setJob({...job,status:"cancelled"}))}>取消</button>}<button className="button primary"><Send size={15}/>送出</button></div></form></aside><aside className="state-panel"><h2>會議狀態</h2>{[["決策",detail.data?.decisions],["未解問題",detail.data?.open_questions],["風險",detail.data?.risks],["行動項目",detail.data?.action_items]].map(([label,items])=><section key={String(label)}><h3>{String(label)} <span>{Array.isArray(items)?items.length:0}</span></h3>{Array.isArray(items)&&items.map(item=><p key={String(item.id)}>{String(item.content)}</p>)}</section>)}</aside></div></div>}
